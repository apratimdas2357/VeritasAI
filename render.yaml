services:
  - type: web
    name: veritas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 4 --preload app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
